services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: tripplanner
      POSTGRES_USER: tripuser
      POSTGRES_PASSWORD: trippassword123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
